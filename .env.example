# PostgreSQL: include credentials and use SSL/TLS for non-local environments.
DATABASE_URL=postgresql://DB_HOST:5432/oqtopus
# Example generator: openssl rand -base64 32
# Required before running any auth-enabled service.
JWT_SECRET=__REQUIRED_SET_A_REAL_SECRET__
