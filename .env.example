# PostgreSQL: provide credentials in the URL below and use SSL/TLS for non-local environments.
DATABASE_URL=postgresql://<user>:<pass>@localhost:5432/oqtopus
# Generate with: openssl rand -base64 32
# Required before running any auth-enabled service.
JWT_SECRET=
