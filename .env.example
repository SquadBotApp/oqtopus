# PostgreSQL: use SSL/TLS for non-local environments (example: postgresql://localhost:5432/oqtopus?sslmode=require)
DATABASE_URL=postgresql://localhost:5432/oqtopus
# Example generator: openssl rand -base64 32
# Required before running any auth-enabled service
JWT_SECRET=
