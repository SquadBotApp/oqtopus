# oqtopus

quantum open source model

## Repository bootstrap defaults

- Template: blank repository (no app template)
- Base ignore rules: Node `.gitignore` + local editor/OS entries
- Local env setup: copy `.env.example` to `.env` and set real values
